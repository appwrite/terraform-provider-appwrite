---
page_title: "Resource: appwrite_mongo_database"
subcategory: "Dedicated Databases"
description: |-
  Manages a dedicated Appwrite MongoDB database. A dedicated database runs on infrastructure reserved for one project, so creating, resizing or upgrading one takes several minutes; Terraform waits for the database to settle before continuing.
---

# Resource: appwrite_mongo_database

Manages a dedicated Appwrite MongoDB database. A dedicated database runs on infrastructure reserved for one project, so creating, resizing or upgrading one takes several minutes; Terraform waits for the database to settle before continuing.

## Example Usage

```terraform
resource "appwrite_mongo_database" "main" {
  name          = "main"
  specification = "db-s-1vcpu-1gb"
}

resource "appwrite_mongo_database" "production" {
  name          = "production"
  specification = "db-s-2vcpu-4gb"

  replicas  = 2
  sync_mode = "quorum"

  pitr                = true
  pitr_retention_days = 30

  storage_autoscaling                   = true
  storage_autoscaling_threshold_percent = 75
  storage_autoscaling_max_gb            = 500

  maintenance_window_day      = "sat"
  maintenance_window_hour_utc = 2
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The database display name.

### Optional

- `id` (String) The database ID. Must be unique within the project. Generated when omitted.
- `idle_timeout_minutes` (Number) Minutes of inactivity before the database container scales to zero. Set to 0 to keep it always on.
- `maintenance_window_day` (String) The day of the week the maintenance window starts. One of `sun`, `mon`, `tue`, `wed`, `thu`, `fri` or `sat`. Must be set together with `maintenance_window_hour_utc`.
- `maintenance_window_hour_utc` (Number) The hour in UTC (0-23) the maintenance window starts. Must be set together with `maintenance_window_day`.
- `network_idle_timeout_seconds` (Number) How long an idle client connection is held open before the server closes it.
- `network_ip_allowlist` (Set of String) IP addresses and CIDR ranges allowed to connect. An empty set allows any address.
- `pitr` (Boolean) Whether point-in-time recovery is enabled.
- `pitr_retention_days` (Number) How many days of point-in-time recovery data to retain.
- `project_id` (String) The Appwrite project ID. Defaults to the provider-level project_id.
- `replicas` (Number) The number of high availability replicas. High availability is enabled when greater than 0.
- `specification` (String) The compute specification slug, for example `db-s-1vcpu-1gb`. Read the available slugs from the corresponding specifications data source. Changing this resizes the database in place.
- `sql_api_allowed_statements` (Set of String) The statement types the SQL API accepts. Defaults to read/write DML only; DDL and DCL types (`CREATE`, `ALTER`, `DROP`, `TRUNCATE`, `GRANT`, `REVOKE`) are opt-in.
- `sql_api_enabled` (Boolean) Whether the SQL API sidecar is enabled, allowing statements to be run over the Appwrite API.
- `sql_api_max_bytes` (Number) The maximum serialized SQL API result payload in bytes. Larger results are truncated.
- `sql_api_max_rows` (Number) The maximum number of rows returned per SQL API execution. Larger results are truncated.
- `sql_api_timeout_seconds` (Number) The maximum server-side SQL API execution time in seconds before a query is canceled.
- `status` (String) The desired database status. Set to `paused` to stop the database without deleting it, and back to `ready` to resume it. Left unset, the status is only read back from the server.
- `storage_autoscaling` (Boolean) Whether storage expands automatically as it fills up.
- `storage_autoscaling_max_gb` (Number) The storage ceiling in GB for autoscaling. 0 means no limit.
- `storage_autoscaling_threshold_percent` (Number) The storage usage percentage that triggers automatic expansion.
- `sync_mode` (String) The replication sync mode. One of `async`, `sync` or `quorum`. Only meaningful when `replicas` is greater than 0.
- `version` (String) The MongoDB engine version. Changing this performs an in-place major version upgrade, which cannot be rolled back.

### Read-Only

- `api` (String) The product API that owns this database.
- `backend` (String) The database backend provider.
- `backup_enabled` (Boolean) Whether automatic backups are enabled.
- `connection_password` (String, Sensitive) The password to connect with. Rotate it out of band; this provider only reads it.
- `connection_port` (Number) The port to connect to.
- `connection_string` (String, Sensitive) The full connection URI, including credentials.
- `connection_user` (String) The username to connect with.
- `container_status` (String) The container status for lifecycle-managed runtimes.
- `cpu` (Number) The allocated CPU in millicores.
- `created_at` (String) The database creation timestamp in ISO 8601 format.
- `engine` (String) The database engine reported by the server.
- `error` (String) The error message reported by the server when the status is `failed`.
- `hostname` (String) The hostname to connect to.
- `idle_until` (String) When the database is expected to be considered idle, in ISO 8601 format.
- `last_accessed_at` (String) The last activity timestamp in ISO 8601 format.
- `lifecycle_state` (String) The idle-lifecycle state: `active`, `warm`, `cold` or `hibernated`.
- `memory` (Number) The allocated memory in MB.
- `metrics_enabled` (Boolean) Whether metrics collection is enabled.
- `network_max_connections` (Number) The maximum number of concurrent client connections.
- `node_pool` (String) The node pool the database is scheduled on.
- `ssl` (Boolean) Whether SSL/TLS is required for client connections.
- `storage` (Number) The allocated storage in GB.
- `storage_class` (String) The storage class backing the volume.
- `storage_max_gb` (Number) The maximum storage allowed in GB. 0 means the system default.
- `updated_at` (String) The database last update timestamp in ISO 8601 format.

## Import

Import is supported using the following syntax:

```shell
terraform import appwrite_mongo_database.main <database-id>
```

## See Also

- [appwrite_mongo_backup_policy](mongo_backup_policy.md) - Schedule backups for the database
- [appwrite_tablesdb](tablesdb.md) - Use Appwrite's shared database infrastructure instead
