---
page_title: "Data Source: appwrite_postgresql_extensions"
subcategory: "Dedicated Databases"
description: |-
  Lists the extensions installed on, and available to, a dedicated Appwrite PostgreSQL database.
---

# Data Source: appwrite_postgresql_extensions

Lists the extensions installed on, and available to, a dedicated Appwrite PostgreSQL database.

## Example Usage

```terraform
data "appwrite_postgresql_extensions" "main" {
  database_id = "main"
}

output "installed_extensions" {
  value = data.appwrite_postgresql_extensions.main.installed
}

# The metadata list explains what each installable extension provides.
output "extension_catalog" {
  value = {
    for e in data.appwrite_postgresql_extensions.main.metadata :
    e.key => "${e.name} (${e.category}): ${e.description}"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `database_id` (String) The dedicated database ID.

### Optional

- `project_id` (String) The Appwrite project ID. Defaults to the provider-level project_id.

### Read-Only

- `available` (Set of String) The extensions that can be installed.
- `installed` (Set of String) The extensions currently installed.
- `metadata` (Attributes List) Curated metadata for each available extension. (see [below for nested schema](#nestedatt--metadata))

<a id="nestedatt--metadata"></a>
### Nested Schema for `metadata`

Read-Only:

- `category` (String) The category the extension belongs to.
- `description` (String) What the extension provides.
- `key` (String) The extension key, as passed to `name` on the extension resource.
- `name` (String) The human readable extension name.

## See Also

- [appwrite_postgresql_extension](../resources/postgresql_extension.md) - Install one of these extensions
