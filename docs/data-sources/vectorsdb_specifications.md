---
page_title: "Data Source: appwrite_vectorsdb_specifications"
subcategory: "VectorsDB"
description: |-
  Lists the compute specifications available for Appwrite VectorsDB databases. This deployment may have no shared pool, in which case `specification` is required when creating a database, so a `specification` slug can be selected without hardcoding it. Availability depends on the organization's billing plan; check `enabled` before using a slug.
---

# Data Source: appwrite_vectorsdb_specifications

Lists the compute specifications available for Appwrite VectorsDB databases. This deployment may have no shared pool, in which case `specification` is required when creating a database, so a `specification` slug can be selected without hardcoding it. Availability depends on the organization's billing plan; check `enabled` before using a slug.

## Example Usage

```terraform
data "appwrite_vectorsdb_specifications" "available" {}

# A deployment without a shared pool rejects a database created with no
# specification, so reading the slugs here avoids hardcoding one the billing
# plan may not allow.
output "available_specifications" {
  value = [for s in data.appwrite_vectorsdb_specifications.available.specifications : s.slug if s.enabled]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `project_id` (String) The Appwrite project ID. Defaults to the provider-level project_id.

### Read-Only

- `specifications` (Attributes List) The available specifications. (see [below for nested schema](#nestedatt--specifications))

<a id="nestedatt--specifications"></a>
### Nested Schema for `specifications`

Read-Only:

- `cpu` (Number) The allocated CPU in millicores.
- `enabled` (Boolean) Whether the specification is available on the current billing plan.
- `included_bandwidth` (Number) The included bandwidth in GB before overage charges apply.
- `included_storage` (Number) The included storage in GB before overage charges apply.
- `max_connections` (Number) The maximum number of concurrent connections.
- `memory` (Number) The allocated memory in MB.
- `name` (String) The human readable specification name.
- `price` (Number) The monthly price in USD.
- `slug` (String) The slug to pass as `specification` when creating a database.

## See Also

- [appwrite_vectorsdb](../resources/vectorsdb.md) - Provision a database on one of these specifications
