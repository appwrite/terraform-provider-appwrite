# Copy this to ~/.terraformrc for local development.
# This lets you skip `terraform init` and use your local build directly.
provider_installation {
  dev_overrides {
    "appwrite/appwrite" = "~/source/terraform-provider"
  }

  direct {}
}
